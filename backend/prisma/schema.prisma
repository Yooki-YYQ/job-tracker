// // This is your Prisma schema file,
// // learn more about it in the docs: https://pris.ly/d/prisma-schema

// // Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// // Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// generator client {
//   provider = "prisma-client-js"
// }

// datasource db {
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }

// // make a User table first
// model User {
//   id        String   @id @default(uuid())
//   email     String   @unique
//   password  String
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
// }

// enum ApplicationStatus {
//   APPLIED
//   INTERVIEW_SCHEDULED
//   INTERVIEWED
//   OFFER_RECEIVED
//   REJECTED
//   WITHDRAWN
//   ACCEPTED
// }

// model Application {
//   id              String             @id @default(uuid())
//   companyName     String
//   positionTitle   String
//   jobUrl          String?            // Optional: Some job links will be closed
//   applicationDate DateTime           @default(now())
//   status          ApplicationStatus  @default(APPLIED)
//   notes           String?            @db.Text

//   createdAt       DateTime           @default(now())
//   updatedAt       DateTime           @updatedAt

//   @@index([status])
//   @@index([applicationDate])
// }
// backend/prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum ApplicationStatus {
  APPLIED
  INTERVIEW_SCHEDULED
  INTERVIEWED
  OFFER_RECEIVED
  REJECTED
  WITHDRAWN
  ACCEPTED
}

model Application {
  id        String    @id @default(uuid())
  data      Json      @default("{}")  // ALL user data goes here
  isDeleted Boolean   @default(false)  // NEW FIELD
  deletedAt DateTime? // NEW FIELD
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  
  // File relationships
  files ApplicationFile[]

  @@map("applications")
}

model ApplicationFile {
  id             String      @id @default(uuid())
  applicationId  String
  fileName       String      // Original filename for display
  filePath       String      // Where file is stored
  uploadedAt     DateTime    @default(now())
  
  application    Application @relation(fields: [applicationId], references: [id], onDelete: Cascade)
  
  @@index([applicationId])
  @@map("application_files")
}